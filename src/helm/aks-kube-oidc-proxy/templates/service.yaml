apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-kube-oidc-proxy.fullname" . }}
  labels:
    {{- include "aks-kube-oidc-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-kube-oidc-proxy.selectorLabels" . | nindent 4 }}
