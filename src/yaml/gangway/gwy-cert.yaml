apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  annotations:
  labels:
    name: gangway
  name: gangway
  namespace: auth
spec:
  dnsNames:
  - gangway.evillgenius.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: gangway-tls